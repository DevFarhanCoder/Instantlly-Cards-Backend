# Environment Variables for Instantlly Cards Backend
# Copy this file to .env and fill in your actual values

# JWT Secret for authentication
JWT_SECRET=your_jwt_secret_here

# MongoDB Atlas (current/fallback database)
MONGODB_URI=mongodb+srv://farhan:farhan90@cluster0.txtst7k.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# AWS DocumentDB (primary database - NEW)
DOCUMENTDB_URI=mongodb://rajeshmodi:InstantllySecure2024!@instantlly-docdb-cluster.cads82eikgcx.us-east-1.docdb.amazonaws.com:27017/instantlly?ssl=true&replicaSet=rs0&readPreference=secondaryPreferred&retryWrites=false

# Expo Push Notifications
EXPO_ACCESS_TOKEN=your_expo_access_token_here

# Server Configuration
PORT=3001
NODE_ENV=production

# Erlang Gateway (optional)
ERLANG_GATEWAY_URL=your_erlang_gateway_url_here

# =============================================================================
# DEPLOYMENT NOTES:
# =============================================================================
# 
# For Local Development:
# - Use MONGODB_URI for MongoDB Atlas connection
# - Set DOCUMENTDB_URI to test DocumentDB (requires VPN/proper network access)
# 
# For Production (Render.com):
# - Set DOCUMENTDB_URI as primary database
# - Keep MONGODB_URI as fallback
# - App will automatically try DocumentDB first, fallback to Atlas if needed
#
# Database Priority:
# 1. DocumentDB (if DOCUMENTDB_URI is set)
# 2. MongoDB Atlas (fallback)
#
# =============================================================================
